# Clippy configuration for strict linting

# Deny warnings in CI
# Set in .cargo/config.toml:
# [target.'cfg(all())']
# rustflags = ["-D", "warnings"]

# Pedantic lints (enable selectively)
# Too strict for all projects, but good to review

# Warn on:
# - Missing documentation
# - Inefficient code patterns
# - Potential bugs
# - Style inconsistencies

# Note: Place this file in project root or .cargo/config.toml
